{
  "name": "@solved/registry1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Heavily rate-limited npm-compatible micro registry for cfnext at registry1.solved.gg",
  "scripts": {
    "build:embed": "bun src/build/embed-package.ts",
    "dev": "bun run build:embed && bun x wrangler dev",
    "deploy": "bun run build:embed && bun x wrangler deploy",
    "types": "bun x wrangler types --env-interface RegistryEnv src/env.d.ts",
    "test": "bun run build:embed && bun test",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.0.0",
    "typescript": "^7.0.0",
    "wrangler": "^4.123.0"
  }
}
